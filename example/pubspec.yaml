name: animated_cycler_example
description: Example app demonstrating AnimatedCycler widget usage
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animated_cycler:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Example assets for demonstration
  assets:
    - assets/images/
    - assets/icons/